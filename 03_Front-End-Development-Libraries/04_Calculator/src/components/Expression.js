import React from "react";
import styled from "styled-components";

const ExpressionContainer = styled.div``;

const Expression = () => {
  return <ExpressionContainer></ExpressionContainer>;
};

export default Expression;
